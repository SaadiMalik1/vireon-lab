version: '3.8'

services:
  vireon:
    build: .
    container_name: vireon-lab
    ports:
      - "7777:7777"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python -m vireon ui --port 7777 --host 0.0.0.0
