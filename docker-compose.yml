version: '3.8'

services:
  neuroshield:
    build: .
    container_name: neuroshield-lab
    ports:
      - "7777:7777"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python -m neuroshield ui --port 7777 --host 0.0.0.0
