[package]
name = "scribe"
version = "0.1.0"
edition = "2024"

[lib]
name = "neuroshield_runemate"
crate-type = ["cdylib"]

[dependencies]
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
pyo3 = { version = "0.20", features = ["extension-module"] }
